# Required: server-side upstream endpoint for Direct Line token generation
BOT2_TOKEN_ENDPOINT=https://YOUR-UPSTREAM-TOKEN-ENDPOINT

# Optional
BOT2_TOKEN_TIMEOUT_SECONDS=10
# Comma-separated list. Leave empty for same-origin local development without CORS middleware.
BACKEND_CORS_ORIGINS=http://localhost:8080
